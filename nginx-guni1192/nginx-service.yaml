apiVersion: v1
kind: Service
namespace: nginx-guni1192
metadata:
  name: nginx-svc
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: nginx
  type: ClusterIP
